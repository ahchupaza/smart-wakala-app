<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.smartwakala">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".RegisterNetSelectionActivity"></activity>
        <activity android:name=".TigoPesaNumVerificationActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".EzyPesaActivity" />
        <activity android:name=".LoginNetSelectionActivity" />
        <activity android:name=".HelpActivity" />
        <activity android:name=".HaloPesaActivity" />
        <activity android:name=".TpesaActivity" />
        <activity android:name=".AirtelMoneyActivity" />
        <activity android:name=".MpesaActivity" />
        <activity android:name=".TigoPesaActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>